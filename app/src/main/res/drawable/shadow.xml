<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape
            android:shape="rectangle">
            <solid android:color="#DDDDDD" /> <!-- Background color of the shadow -->
            <corners android:radius="4dp" /> <!-- Adjust the corner radius as needed -->
        </shape>
    </item>
    <item
        android:right="1dp"
        android:left="1dp"
        android:top="1dp"
        android:bottom="1dp">
        <shape
            android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Background color of the view -->
            <corners android:radius="4dp" /> <!-- Adjust the corner radius as needed -->
        </shape>
    </item>
</layer-list>
